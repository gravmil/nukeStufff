# Core (required)
numpy>=1.23
scipy>=1.9
imageio>=2.31
tifffile>=2023.7.10

# Optional but strongly recommended: real EXR read/write (best precision,
# the standard format in VFX/texturing pipelines). Falls back to 32-bit
# TIFF automatically if this isn't installed.
OpenEXR>=3.2
